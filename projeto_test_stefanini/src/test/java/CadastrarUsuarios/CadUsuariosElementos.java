package CadastrarUsuarios;

import java.util.List;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;

public class CadUsuariosElementos {
	
	
	static WebDriver driver;
	
	
	protected WebElement  name;// = driver.findElement(By.id("name"));
	protected WebElement  email;// = driver.findElement(By.id("email"));
	protected WebElement  password;// = driver.findElement(By.id("password"));
	protected WebElement  register;// = driver.findElement(By.id("register"));
	protected WebElement  validamsg;
	protected WebElement  removeUser1;// = driver.findElement(By.id("removeUser1"));
	//protected WebElement tabela;
	protected List<WebElement> tabela;
	


}
